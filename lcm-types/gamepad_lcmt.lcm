struct gamepad_lcmt {
    int32_t leftBumper, rightBumper, leftTriggerButton, rightTriggerButton, back, start, a,b,x,y,leftStickButton, rightStickButton, buttonUp, buttonDown,buttonLeft, buttonRight;
    float leftTriggerAnalog, rightTriggerAnalog;
    float leftStickAnalog[2];
    float rightStickAnalog[2];
}
